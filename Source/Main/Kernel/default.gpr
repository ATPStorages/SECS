project Default is

   package Compiler is
      for Switches ("ada") use ("-g", "-O2");
   end Compiler;

   package Ide is
      for Vcs_Kind use "git";
      for Vcs_Repository_Root use "../../../../SECS";
   end Ide;

end Default;

